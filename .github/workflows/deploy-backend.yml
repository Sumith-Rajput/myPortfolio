name: Deploy Backend to Azure

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: ${{ secrets.AZURE_WEBAPP_NAME }}
  NODE_VERSION: '18.x'

jobs:
  validate-secrets:
    name: Validate Secrets
    runs-on: ubuntu-latest
    steps:
      - name: Check Required Secrets
        run: |
          echo "🔍 Validating required secrets for backend deployment..."
          missing_secrets=()
          
          if [ -z "${{ secrets.AZURE_WEBAPP_NAME }}" ]; then
            echo "❌ AZURE_WEBAPP_NAME is missing"
            missing_secrets+=("AZURE_WEBAPP_NAME")
          else
            echo "✅ AZURE_WEBAPP_NAME is set: ${{ secrets.AZURE_WEBAPP_NAME }}"
          fi
          
          if [ -z "${{ secrets.AZURE_RESOURCE_GROUP }}" ]; then
            echo "❌ AZURE_RESOURCE_GROUP is missing"
            missing_secrets+=("AZURE_RESOURCE_GROUP")
          else
            echo "✅ AZURE_RESOURCE_GROUP is set: ${{ secrets.AZURE_RESOURCE_GROUP }}"
          fi
          
          if [ -z "${{ secrets.AZURE_CREDENTIALS }}" ]; then
            echo "❌ AZURE_CREDENTIALS is missing"
            missing_secrets+=("AZURE_CREDENTIALS")
          else
            echo "✅ AZURE_CREDENTIALS is set"
          fi
          
          if [ ${#missing_secrets[@]} -gt 0 ]; then
            echo ""
            echo "⚠️  Missing secrets: ${missing_secrets[*]}"
            echo "📝 Please add these secrets in: Settings → Secrets and variables → Actions"
            exit 1
          else
            echo ""
            echo "✅ All required backend secrets are configured!"
          fi

  build-and-deploy:
    needs: validate-secrets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        working-directory: ./backend
        run: npm install --production

      - name: Verify backend files
        run: |
          echo "📁 Verifying backend files..."
          ls -la backend/
          if [ -f "backend/server.js" ]; then
            echo "✅ server.js exists"
          else
            echo "❌ server.js not found!"
            exit 1
          fi
          if [ -f "backend/package.json" ]; then
            echo "✅ package.json exists"
          else
            echo "❌ package.json not found!"
            exit 1
          fi
          if [ -f "backend/data.json" ]; then
            echo "✅ data.json exists"
          else
            echo "❌ data.json not found!"
            exit 1
          fi

      - name: Create deployment package
        run: |
          cd backend
          zip -r ../backend-deploy.zip . -x "*.git*" -x "*node_modules/.cache*"
          echo "✅ Created backend-deploy.zip"
          ls -lh ../backend-deploy.zip

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          package: ./backend-deploy.zip
          startup-command: 'node server.js'

      - name: Restart Azure Web App
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az webapp restart --name ${{ env.AZURE_WEBAPP_NAME }} --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}

